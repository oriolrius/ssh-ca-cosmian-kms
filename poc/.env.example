# Copy to .env and adjust if the defaults clash with your machine.
# Local published port for the PoC sshd (host side).
SSH_PORT=2222
# Lab network subnet.
LAB_SUBNET=10.222.9.0/24
# KMS path (optional overlay).
KMS_IMAGE=ghcr.io/cosmian/kms:latest
KMS_PORT=9998
KRL_PORT=8088
